{
  "name": "mandrips",
  "scripts": {
    "dev": "bun run index.ts",
    "check": "oxlint --fix && oxfmt --write"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "oxfmt": "^0.55.0",
    "oxlint": "^1.85.0",
    "wrangler": "^4.140.0"
  }
}
